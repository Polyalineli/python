Ќапишите проверку число на простoту (число €вл€етс€ простым, если оно делитс€ нацело только на себ€ и единицу)

is_prime = True
n = (int(input()))
for i in range(2,n):
    if n % i == 0:
	is_prime = False
	print("composite number")
	break
if is_prime == True:
    print("prime number")

¬ыведете все простые числа дл€ заданного интервала

is_prime = True
n = (int(input()))
sum = 0
for i in range(2,n):
    if n % i == 0:
	is_prime = False
	break
if is_prime == True:
    sum += i
print(sum)

¬ыведете все числа в заданном интервале, позици€ которых четна/нечетна (режим работы должен определ€тс€ из некоторой переменной котора€ при значении True должны выводить числа сто€щие на четной позиции и наоборот).

x = int(input("please enter numer for me"))
y = 50
for i in range(int(input("now enter range of calculation"))):
    if x < y:
        if (i % 2) == 0:
            print(i)
    else:
        if (i % 2)!= 0:
            print(i)

«апустите вечный цикл при этом на каждом шаге цикла просите ввести некоторое значение пользовател€ (команда input(...)), если пользователь ввел букву q то ваша программа должна завершатьс€

while input() != 'q':
    input()

Ќайдите сумму всех четных элементов р€да ‘ибоначчи, которые не превышают четыре миллиона.

i = 0
j = 1
k = i + j
h = 0
while  k < 4000000:
        k = i + j
        i = j
        j = k    
        if (k % 2) == 0: 
            h += k
print(h)

Ќайдите сумму всех чисел меньше 1000, кратных 3 или 5.

h = 0
for i in range(10000):
    if (i % 3) == 0 or (i % 5) == 0:
        h +=i
print(h)

Ќайдите все тройки пифагора дл€ заданного интервала

i = int(input())
for a in range(1, i+1):
    for b in range(1, i+1):
        for c in range(1, i+1):
            if a*a + b*b == c*c:
                print(a, b, c)